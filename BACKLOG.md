# 📝 BACKLOG.md — LeaveEasy

งานที่เหลือของ Module 2 (สัปดาห์ 8-9) และ backlog ที่เลื่อนไป Module 3 ตาม `leaveeasy-spec.md` — เอกสารนี้ไม่ใช่ที่ให้เริ่มลงมือเอง แต่ไว้กันลืมและกันทำล่วงหน้าโดยไม่ตั้งใจ (ดูหัวข้อ 8-9 ของสเปคก่อนเริ่มแต่ละข้อเสมอ)

---

## สัปดาห์ที่ 8 — ยังไม่ทำ

- [ ] **Security Rules รายบทบาทครบชุดตามเช็กลิสต์ 5 ข้อ** (สเปคหัวข้อ 8) — แยกสิทธิ์ตาม `employee` / `manager` / `hr` ครอบทุกโฟลเดอร์รวม subcollection `approvals`
- [ ] **ทดสอบ Security Rules ด้วยการสวมรอย** (impersonation) — ยืนยันว่าผู้ขอลาคนหนึ่งเปิดใบลาของอีกคนไม่ได้จริง (มีเทสต์ baseline ที่ยืนยันว่า "ยังเปิดได้" อยู่แล้วใน `test-results.md` — ใช้เทสต์เดิมมาเช็คซ้ำหลังทำข้อนี้เสร็จ ควรได้ผล 403/เปิดไม่ได้)
- [ ] **ปุ่ม "ให้ AI ช่วยจัดประเภทการลา" ผ่าน OpenRouter** (US-09) — ต้องมี timeout 15 วิ, ป้ายกำกับ "ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน", ผลลัพธ์ต้องเป็นประเภทที่มีอยู่จริงเท่านั้น, ห้ามค้างถ้าเรียกไม่สำเร็จ — **ระวังเรื่องคีย์ OpenRouter: ต้องเป็นความลับจริง ห้าม hardcode ในโค้ด `.js` ที่ commit ขึ้น GitHub** (ต่างจาก Firebase apiKey ที่ไม่ใช่ความลับ)
- [x] **Reviewer agent ตรวจโค้ดและกฎ** — สร้างแล้วที่ `.claude/agents/reviewer.md`
- [ ] ซ่อนหน้า `leave-types.html` จากผู้ที่ไม่ใช่ฝ่ายบุคคล (ตามตาราง "เพิ่มทีหลัง" ของหน้าที่ 4 ในหัวข้อ 4)
- [ ] นับตัวเลขในแดชบอร์ดเฉพาะที่ผู้ใช้คนนั้นมีสิทธิ์เห็น — **รอก่อน**: แดชบอร์ดยังใช้ข้อมูลปลอมตลอด Module 2 ตามที่สเปคระบุไว้ชัด (US-11 เป็นงาน Module 3) ข้อนี้จะเกี่ยวข้องจริงก็ต่อเมื่อ Module 3 เริ่มต่อข้อมูลจริงแล้ว

## สัปดาห์ที่ 9 — ยังไม่ทำ

- [x] **Tester + Playwright MCP** — สร้างแล้วที่ `.claude/agents/tester.md` ทดสอบสัปดาห์ 6-7 ผ่านแล้ว (ดู `test-results.md`)
- [ ] รันเทสต์ชุดสัปดาห์ 8 ซ้ำ (Security Rules รายบทบาท + ปุ่ม AI) หลังสร้างเสร็จ
- [ ] แก้บั๊กที่เทสต์จับได้ (ยังไม่พบบั๊กจริงจนถึงตอนนี้ ดู `test-results.md`)
- [ ] เตรียมคลิปสาธิต 3 นาที
- [ ] ซ้อมอธิบายระบบก่อน Consult — ใช้ `.claude/agents/consultant.md` ช่วยซ้อม

---

## Backlog / เลื่อนไป Module 3 (ตามสเปคหัวข้อ 3 และ 9 — ห้ามทำใน Module 2)

- [ ] **US-10 — ค้นหา กรอง เรียงลำดับ** ในหน้ารายการใบลา (ช่องค้นหา, ตัวกรองสถานะ/ประเภท, สลับเรียงใหม่↔เก่า)
- [ ] **US-11 — แดชบอร์ดสรุปด้วยข้อมูลจริง** พร้อมกราฟ (ตอนนี้เป็นโครงหน้าจอจาก prototype เท่านั้นตลอด Module 2)
- [ ] **US-12 — แนบเอกสารประกอบใบลา** ผ่าน Firebase Storage (`attachmentUrl`)
- [ ] แบ่งหน้ารายการ (pagination) — ตอนนี้จำกัดจำนวนที่ดึงมาแสดงแทน
- [ ] คำนวณวันลาคงเหลือ/โควตาวันลาประจำปี
- [ ] ปฏิทินการลาของทีม + ตรวจวันลาชนกัน
- [ ] แจ้งเตือนทางอีเมล/LINE/SMS
- [ ] ส่งออกรายงานเป็น Excel/PDF
- [ ] การอนุมัติหลายขั้น/สายบังคับบัญชาหลายชั้น
- [ ] เก็บประวัติการแก้ไข (audit log)
- [ ] ทดสอบอัตโนมัติแบบ CI (รันเทสต์ทุกครั้งที่ push)

---

> 📌 เอกสารนี้อัปเดตล่าสุดหลังสัปดาห์ที่ 7 (2026-09-20) — เพิ่มรายการเมื่อทำสัปดาห์ 8-9 เสร็จแต่ละข้อ อย่าลบรายการ backlog ที่ยังไม่ทำ แค่ติ๊กถูกเมื่อเสร็จจริง
