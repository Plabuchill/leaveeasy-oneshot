# ผลทดสอบ LeaveEasy — ชุดทดสอบ 5 ข้อ (2026-09-20)

ทดสอบผ่านเบราว์เซอร์จริงด้วย Playwright MCP กับเซิร์ฟเวอร์ทดสอบ `npx serve -l 3000 .` ที่รันบนเครื่องนี้ ต่อกับ Firebase project จริง (`leaveeasy-oneshot-390ee`) ตามที่ตั้งค่าไว้ใน `js/firebase-config.js` (ไม่ใช่ mock)

บัญชีที่ใช้ทดสอบ (สร้างใหม่ทั้งคู่ในรอบนี้ ไม่ใช้บัญชีเก่า — รหัสผ่านไม่บันทึกไว้ในรายงานนี้เพราะบัญชียังไม่ถูกลบและเว็บยังออนไลน์อยู่):
- `tester-agent-20260920-1@example.com` — ชื่อ "Tester Agent One"
- `tester-agent-20260920-2@example.com` — ชื่อ "Tester Agent Two"

ใบลาที่สร้างระหว่างทดสอบ: เอกสาร Firestore `leaveRequests/EOnjFGCmY9wB6DdvXqOB` หัวข้อ `[TESTER] ยื่นใบลา-ทดสอบ1` (สร้างโดย Tester Agent One)

---

## สรุปผล

| ทดสอบ | ตรวจอะไร | ผล | วันเวลาที่รัน |
|---|---|---|---|
| Test 1 | ยื่นใบลาแล้วเห็นในรายการ (US-02+US-01) | **ผ่าน** | 2026-09-20 21:25 |
| Test 2 | กดอนุมัติแล้วสถานะเปลี่ยน + persist จริง (US-04) | **ผ่าน** | 2026-09-20 21:25–21:26 |
| Test 3 | เว้นหัวข้อว่างแล้วกดบันทึก ต้องไม่บันทึก | **ผ่าน** | 2026-09-20 21:26 |
| Test 4 | ไม่ล็อกอินแล้วอ่านข้อมูลไม่ได้ (US-08) | **ผ่าน** | 2026-09-20 21:26–21:27 |
| Test 5 | เปิดใบลาของคนอื่นด้วยบัญชีที่สอง (isolation) | **ผ่านตามที่คาดไว้ว่ายังทำไม่ได้** — เปิดได้จริง ตรงกับ known gap ของสัปดาห์ที่ 8 (ไม่ใช่บั๊กของแอป) | 2026-09-20 21:27 |

---

## รายละเอียดแต่ละข้อ

### Test 1 — ยื่นใบลาแล้วเห็นในรายการ (US-02 + US-01)

**ตรวจ:** ล็อกอิน (สมัครใหม่) → เข้า `new-leave-request.html` → กรอกฟอร์มครบทุกช่อง หัวข้อ `[TESTER] ยื่นใบลา-ทดสอบ1` → กดบันทึก → ต้องเด้งไปหน้ารายการ และเห็นแถวใหม่สถานะ "รอพิจารณา" พร้อมประเภทการลาถูกต้อง

**ผล: ผ่าน** (2026-09-20 21:25)
- สมัครสมาชิก `tester-agent-20260920-1@example.com` สำเร็จ → เด้งไป `index.html` อัตโนมัติ (มีไฟล์ `users/{uid}` ถูกสร้างจริงตามที่ `js/auth.js` ทำ)
- หน้า `new-leave-request.html` โหลดตัวเลือกประเภทการลาจาก Firestore จริง (ลาพักร้อน / ลาป่วย / ลากิจ) ตรงกับข้อมูล seed `leaveTypes`
- กรอกครบทุกช่อง (หัวข้อ, เหตุผล, ประเภท=ลากิจ, วันที่เริ่ม/สิ้นสุด 2026-09-25) → กดบันทึก → เด้งไป `leave-requests.html` ทันที
- แถวใหม่ปรากฏในตาราง: หัวข้อ `[TESTER] ยื่นใบลา-ทดสอบ1` · ประเภท `ลากิจ` · สถานะ `รอพิจารณา` (ป้ายสีเหลืองถูกต้อง) · ผู้ขอลา `Tester Agent One` (ชื่อไทย/ชื่อจริง ไม่ใช่ uid) · วันที่ลา `2026-09-25 ถึง 2026-09-25`
- ไม่มี console error สีแดงที่เกี่ยวกับแอป (มีแค่ favicon 404 ซึ่งไม่เกี่ยวกับฟีเจอร์)

### Test 2 — กดอนุมัติแล้วสถานะเปลี่ยน (US-04)

**ตรวจ:** เปิดหน้ารายละเอียดของใบที่สร้างใน Test 1 → ต้องมีปุ่มอนุมัติ/ไม่อนุมัติ → กด "อนุมัติ" → ป้ายสถานะเปลี่ยนทันที → reload หน้าแล้วค่ายังอยู่ (พิสูจน์ว่าบันทึกจริงใน Firestore ไม่ใช่แค่ในหน่วยความจำ) → ปุ่มต้องหายไป

**ผล: ผ่าน** (2026-09-20 21:25–21:26)
- เปิด `leave-request-detail.html?id=EOnjFGCmY9wB6DdvXqOB` (id จริงจาก Firestore หลังบันทึกใน Test 1) — แสดงครบทุกช่องถูกต้อง รวมถึง "ผู้อนุมัติ: ยังไม่ได้กำหนดผู้อนุมัติ" (ตรงตามเกณฑ์ US-03)
- มีปุ่ม "อนุมัติ" / "ไม่อนุมัติ" / "ลบใบลานี้" ครบเพราะสถานะยัง "รอพิจารณา"
- กด "อนุมัติ" → ป้ายสถานะเปลี่ยนเป็น "อนุมัติ" ทันทีโดยไม่ต้อง reload และปุ่มทั้ง 3 หายไปทันที แทนที่ด้วยข้อความ "ใบนี้พิจารณาแล้ว จึงเปลี่ยนสถานะต่อไม่ได้"
- Reload หน้าเดิม (`navigate` ไป URL เดิมอีกครั้ง) → สถานะยังเป็น "อนุมัติ" และไม่มีปุ่มอนุมัติ/ไม่อนุมัติ/ลบกลับมา → ยืนยันว่าบันทึกจริงใน Firestore ไม่ใช่แค่ state ในหน่วยความจำของหน้าเดิม
- ไม่มี console error สีแดง

### Test 3 — เว้นหัวข้อว่างแล้วกดบันทึก ต้องไม่บันทึกและมีคำเตือน

**ตรวจ:** เข้า `new-leave-request.html` อีกครั้ง กรอกทุกช่องยกเว้นหัวข้อ กดบันทึก → (ก) ต้องไม่เด้งหน้า + มีคำเตือนสีแดงขึ้น (ข) ต้องไม่มีเอกสารใหม่เกิดขึ้นจริงในฐานข้อมูล (ตรวจผ่านหน้ารายการ)

**ผล: ผ่าน** (2026-09-20 21:26)
- กรอกเหตุผล/ประเภท(ลาพักร้อน)/วันที่เริ่ม-สิ้นสุด (2026-09-26) แต่เว้นช่องหัวข้อว่างไว้ → กดบันทึก
- หน้าไม่เด้งไปไหน (ยังอยู่ที่ `new-leave-request.html`) และมีข้อความเตือนสีแดง (`class="alert alert-error"`) ขึ้นว่า "⚠️ กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก"
- ตรวจหน้า `leave-requests.html` หลังจากนั้น — มีแถวเท่าเดิม 6 แถว (5 seed + 1 จาก Test 1) ไม่มีแถวใหม่จาก Test 3 ปรากฏเลย ยืนยันว่าไม่มีเอกสารใหม่ถูกสร้างจริง
- ไม่มี console error สีแดง

### Test 4 — ไม่ล็อกอินแล้วเปิดหน้ารายการ ต้องอ่านข้อมูลไม่ได้ (US-08)

**ตรวจ:** ออกจากระบบ แล้วเข้า `leave-requests.html` ตรง ๆ ต้องเด้งไป `login.html` และแยกต่างหาก ทดสอบยิง Firestore REST API ตรง ๆ แบบไม่ล็อกอิน ต้องได้ 403 PERMISSION_DENIED จริงจากเซิร์ฟเวอร์

**ผล: ผ่าน** (2026-09-20 21:26–21:27)
- กด "ออกจากระบบ" → เด้งไป `login.html` ทันที
- นำทางตรงไปที่ `http://localhost:3000/leave-requests.html` ทั้งที่ไม่ได้ล็อกอิน → ถูก `js/auth.js` เด้งกลับไป `login.html` ทันที (ตรงตามกฎขั้นต่ำ "ต้องล็อกอินก่อน")
- ยิง `fetch()` แบบไม่ใส่ token ไปที่ `https://firestore.googleapis.com/v1/projects/leaveeasy-oneshot-390ee/databases/(default)/documents/leaveRequests` จาก browser console (หน้า login.html) → ได้ผลลัพธ์จริงจากเซิร์ฟเวอร์:
  ```
  status: 403
  body: {"error":{"code":403,"message":"Missing or insufficient permissions.","status":"PERMISSION_DENIED"}}
  ```
  ยืนยันว่า `firestore.rules` (กฎขั้นต่ำ "ต้องล็อกอินก่อน") ทำงานถูกต้องจริงฝั่งเซิร์ฟเวอร์ ไม่ใช่แค่ UI กันไว้
- Console มี error 403 ของ fetch ที่ตั้งใจยิงเองปรากฏ (เป็นผลลัพธ์ที่คาดหวังของการทดสอบ ไม่ใช่บั๊ก) และ favicon 404 ตามปกติ ไม่มี error อื่นที่ผิดปกติ

### Test 5 — ล็อกอินด้วยบัญชีที่สอง แล้วเปิดใบลาของบัญชีแรก ต้องเปิดไม่ได้ (per-owner isolation)

**ตรวจ:** สมัครบัญชีที่สอง ล็อกอิน แล้วเข้า URL หน้ารายละเอียดของใบลาจาก Test 1 ตรง ๆ ด้วย doc id จริง บันทึกว่าเปิดได้หรือไม่ และยิง Firestore REST ด้วย ID token ของบัญชีที่สองอ่านเอกสารเดียวกัน บันทึกผลลัพธ์

**ผล: เปิดได้จริง — ตรงกับที่คาดไว้ว่ายังเป็นช่องว่างของสัปดาห์ที่ 8 ไม่ใช่บั๊กของสัปดาห์ที่ 7** (2026-09-20 21:27)

> ตาม `leaveeasy-spec.md` หัวข้อ 8: การจำกัดสิทธิ์แบบแยกเจ้าของ (per-requester isolation) เป็นขอบเขตของสัปดาห์ที่ 8 ส่วน `firestore.rules` ปัจจุบัน (สัปดาห์ที่ 7) มีกฎขั้นต่ำแค่ "ต้องล็อกอินก่อน" เท่านั้น ยังไม่ตรวจความเป็นเจ้าของเอกสาร ผลที่ได้จึงเป็นไปตามที่คาดไว้ ไม่ใช่ FAIL ของงานสัปดาห์นี้ — บันทึกไว้เป็น baseline สำหรับตรวจซ้ำหลังทำสัปดาห์ที่ 8

- สมัครบัญชีที่สอง `tester-agent-20260920-2@example.com` สำเร็จ → ล็อกอินเป็น "Tester Agent Two"
- นำทางตรงไปที่ `http://localhost:3000/leave-request-detail.html?id=EOnjFGCmY9wB6DdvXqOB` (ใบลาของบัญชีแรก) → **เปิดได้เต็มรูปแบบ** เห็นข้อมูลครบทุกช่อง (หัวข้อ, เหตุผล, ประเภท, วันที่, ชื่อผู้ขอลา "Tester Agent One", สถานะ "อนุมัติ") ไม่มีการกันสิทธิ์ใด ๆ ในหน้าเว็บ
- ยิง Firestore REST ตรง ๆ ด้วย ID token ของบัญชีที่สอง (`window.auth.currentUser.getIdToken()`) อ่านเอกสารเดียวกัน → ได้ผลลัพธ์:
  ```
  status: 200
  body: { ...fields ครบทุกช่องของเอกสาร รวม requesterId ของบัญชีแรก... }
  ```
  ยืนยันว่าฝั่งเซิร์ฟเวอร์ (ไม่ใช่แค่ UI) ก็ยังอนุญาตให้ผู้ใช้ที่ล็อกอินคนไหนก็ได้อ่านเอกสารของคนอื่นได้ ตรงกับกฎ `allow read, write: if request.auth != null;` ใน `firestore.rules` ปัจจุบันเป๊ะ ๆ
- ไม่มี console error สีแดงที่ผิดปกติ

---

## บั๊กที่พบ

ไม่พบบั๊กของแอปที่อยู่ในขอบเขตสัปดาห์ที่ 7 ทั้ง 5 ข้อทดสอบทำงานตามที่คาดไว้ครบถ้วน (รวมถึง Test 5 ที่ผลลัพธ์ตรงกับ known gap ของสัปดาห์ที่ 8 ตามที่ระบุไว้ล่วงหน้า ไม่ใช่บั๊ก)

สิ่งที่สังเกตเห็นแต่ไม่ใช่บั๊ก (บันทึกไว้เป็นข้อมูลอ้างอิงสำหรับสัปดาห์ที่ 8):
- Security Rules ปัจจุบันเป็น `allow read, write: if request.auth != null;` ครอบคลุมทุกโฟลเดอร์ (ยืนยันด้วย Test 4 และ Test 5 ทั้งฝั่ง UI และฝั่ง REST ตรง ๆ) — ผู้ใช้ที่ล็อกอินคนไหนก็เปิด/แก้/ลบเอกสารของคนอื่นได้หมด ต้องแก้ในสัปดาห์ที่ 8 ตามแผน

## การทำความสะอาดหลังทดสอบ

- พยายามลบใบลาทดสอบ (`leaveRequests/EOnjFGCmY9wB6DdvXqOB`, หัวข้อ `[TESTER] ยื่นใบลา-ทดสอบ1`) ผ่าน UI ไม่ได้ เพราะ **Test 2 อนุมัติใบนี้ไปแล้ว** ทำให้สถานะเป็น "อนุมัติ" และตามกฎ US-07 ปุ่มลบจะกดได้เฉพาะใบที่สถานะยังเป็น "รอพิจารณา" เท่านั้น หน้ารายละเอียดจึงไม่มีปุ่มลบให้กดอีกต่อไป — ต้องให้มนุษย์ลบเอกสารนี้ผ่าน Firebase Console เอง
- บัญชีทดสอบทั้งสองยังอยู่ใน Firebase Authentication + มีไฟล์ `users/{uid}` ของทั้งคู่ ต้องให้มนุษย์ลบทีหลัง:
  - `tester-agent-20260920-1@example.com` (uid ปรากฏใน field `requesterId` ของเอกสารด้านบน: `tFxHaMwhmkYLnvRbXkplQcZ2wC52`)
  - `tester-agent-20260920-2@example.com`
- ไม่ได้แตะต้อง/ลบ/แก้ข้อมูล seed ใด ๆ (`lr001`–`lr005`, `u001`–`u003`, `lt001`–`lt003`) ตลอดการทดสอบ

## รายการที่ต้องให้มนุษย์ลบ/จัดการต่อผ่าน Firebase Console

1. เอกสาร Firestore `leaveRequests/EOnjFGCmY9wB6DdvXqOB` (หัวข้อ `[TESTER] ยื่นใบลา-ทดสอบ1`)
2. บัญชี Firebase Auth + เอกสาร `users/tFxHaMwhmkYLnvRbXkplQcZ2wC52` ของ `tester-agent-20260920-1@example.com`
3. บัญชี Firebase Auth + เอกสาร `users/{uid}` ของ `tester-agent-20260920-2@example.com` (uid ไม่ได้บันทึกไว้ระหว่างทดสอบ ต้องค้นหาจากอีเมลใน Firebase Console)
