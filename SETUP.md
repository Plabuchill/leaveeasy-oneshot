# 🔧 SETUP.md — ตั้งค่า Firebase ให้ LeaveEasy (สัปดาห์ที่ 6)

ทำตามลำดับนี้ทีละขั้น อย่าข้ามขั้น

1. สร้างโปรเจกต์ที่ https://console.firebase.google.com
2. เปิดใช้ Firestore Database ในโหมดทดสอบ (test mode)
3. เพิ่มเว็บแอปในโปรเจกต์แล้วคัดลอกค่า Firebase config
4. วางค่า config ที่คัดลอกมาแทนที่ placeholder ใน `js/firebase-config.js`
5. เปิดไฟล์ `seed.html` ในเบราว์เซอร์แล้วกดปุ่มใส่ข้อมูลตัวอย่าง — ทำครั้งเดียวพอ
6. เปิด `leave-requests.html` แล้วตรวจว่าเห็นใบลา 5 ใบจริงจาก Firestore ลองแก้ค่าใน Firebase Console แล้วรีเฟรชหน้าเว็บดูว่าเปลี่ยนตาม

---

## รายละเอียดแต่ละขั้น

### 1. สร้างโปรเจกต์ที่ Firebase Console

เปิด https://console.firebase.google.com แล้วกด **"สร้างโปรเจกต์" (Add project)** ตั้งชื่ออะไรก็ได้ (เช่น `leaveeasy-ชื่อคุณ`) แล้วรอจนสร้างเสร็จ ปิดการใช้ Google Analytics ได้ ไม่จำเป็นสำหรับ Module นี้

### 2. เปิดใช้ Firestore Database (โหมดทดสอบ)

ในเมนูซ้ายของโปรเจกต์ เปิด **Firestore Database** → กด **สร้างฐานข้อมูล (Create database)** → เลือก **โหมดทดสอบ (Start in test mode)** → เลือกตำแหน่งเซิร์ฟเวอร์ (region) อะไรก็ได้ที่ใกล้ที่สุด

> ⚠️ โหมดทดสอบเปิดให้อ่าน/เขียนได้โดยไม่ต้องล็อกอิน ใช้ได้ถึงสัปดาห์ที่ 7 เท่านั้น (สัปดาห์ที่ 7 จะเปลี่ยนเป็นกฎ "ต้องล็อกอินก่อน")

### 3. เพิ่มเว็บแอปแล้วคัดลอกค่า config

กดรูปเฟือง ⚙️ ข้างคำว่า "Project Overview" → **Project settings** → เลื่อนลงมาที่ **"Your apps"** → กดไอคอน **`</>`** เพื่อเพิ่มเว็บแอป (Add app) → ตั้งชื่อแอปอะไรก็ได้ → กด **Register app**

หน้าจอจะโชว์ก้อนโค้ดที่มีตัวแปร `firebaseConfig` หน้าตาแบบนี้:

```js
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
```

คัดลอกค่าทั้งก้อนนี้เก็บไว้ก่อน (ใช้ในขั้นที่ 4)

### 4. วางค่า config ลงใน js/firebase-config.js

เปิดไฟล์ `js/firebase-config.js` แล้วแทนที่ก้อน placeholder ในตัวแปร `window.firebaseConfig` ด้วยค่าจริงที่คัดลอกมาจากขั้นที่ 3 บันทึกไฟล์

### 5. ใส่ข้อมูลตัวอย่างด้วย seed.html

เปิดไฟล์ `seed.html` ในเบราว์เซอร์ (บอก Claude ว่า "ช่วยเปิดเว็บนี้ให้ผมดูในเบราว์เซอร์" แล้วเข้าที่อยู่ที่มันบอก ต่อท้ายด้วย `/seed.html`) แล้วกดปุ่ม **"ใส่ข้อมูลตัวอย่างลง Firestore"**

หน้าจอจะขึ้นข้อความสีเขียวเมื่อสำเร็จ กดปุ่มซ้ำได้หลายครั้งโดยไม่ทำให้ข้อมูลซ้ำ (ใช้รหัสเอกสารคงที่เดิมทุกครั้ง)

หน้านี้เป็นเครื่องมือใช้ครั้งเดียวตอนติดตั้ง ไม่ใช่หนึ่งใน 5 หน้าจอหลักของระบบ จึงไม่มีลิงก์มาจากเมนู

### 6. ตรวจว่าหน้ารายการอ่านข้อมูลจริง

เปิด `leave-requests.html` ต้องเห็นตารางใบลา 5 ใบ (ตรงกับข้อมูลใน `leaveeasy-spec.md` หัวข้อ 7.3)

ลองเข้า Firebase Console → Firestore Database → เปิดโฟลเดอร์ `leaveRequests` → แก้ค่าช่องไหนก็ได้ของเอกสารไหนก็ได้ (เช่นเปลี่ยน `title`) → กลับมารีเฟรชหน้า `leave-requests.html` → ต้องเห็นค่าที่แก้เปลี่ยนตามทันที ถ้าเปลี่ยนตาม แปลว่าเชื่อมต่อสำเร็จแล้ว

---

## ถ้าเจอปัญหา

- เห็นแถบเหลือง "⚠️ ยังไม่ได้ตั้งค่า Firebase" ที่หน้า `leave-requests.html` → กลับไปตรวจขั้นที่ 4 ว่าใส่ค่า config ครบและถูกต้องหรือยัง
- หน้า `seed.html` ขึ้นข้อความสีแดง → เปิด Console ของเบราว์เซอร์ (กด F12) ดูรายละเอียด error มักเกิดจากค่า `projectId` ผิด หรือยังไม่ได้เปิดใช้ Firestore Database ในขั้นที่ 2

---

## สัปดาห์ที่ 7 — ล็อกอิน + ขึ้นออนไลน์

การสมัครสมาชิก (signup) และล็อกอิน (login) ถูกดำเนินการแยกต่างหากผ่านโค้ด js/auth.js และหน้าจอ signup.html / login.html — ตัวอักษรขั้นตอนนี้จัดการส่วน Firebase CLI, Hosting, และ Security Rules เท่านั้น ทำตามลำดับนี้ทีละขั้น

> ⚠️ **ก่อนขั้นที่ 1** ถ้าคุณยังไม่ได้รันเครื่องมือ `seed.html` จากสัปดาห์ที่ 6 และกดปุ่มใส่ข้อมูลตัวอย่าง ให้ทำตอนนี้เลยก่อนไป — เพราะเมื่อปรับใช้กฎความปลอดภัย Security Rules ในขั้นที่ 5 ต่อไป เครื่องมือ `seed.html` (ซึ่งไม่มีระบบล็อกอินของตัวเอง) จะเขียนข้อมูลลงฐานข้อมูลไม่ได้อีก (อ่านเขียนทั้งหมดจะต้องล็อกอินก่อน)

### 0. เปิดใช้ Firebase Authentication (ทำครั้งเดียว ผ่าน Console เท่านั้น)

ก่อนสมัครสมาชิก/ล็อกอินจะทำงานได้เลย ต้องเปิดฟีเจอร์นี้ในโปรเจกต์ก่อน — ถ้าข้ามขั้นนี้จะเจอ error `auth/configuration-not-found` ตอนกดสมัครสมาชิก

1. เปิด Firebase Console ของโปรเจกต์คุณ → เมนูซ้าย เปิด **Build → Authentication**
2. กด **"Get started"**
3. ในแท็บ **Sign-in method** เลือกผู้ให้บริการ **อีเมล/รหัสผ่าน (Email/Password)** แล้วกด **Enable** → บันทึก

ไม่มีคำสั่ง Firebase CLI ที่เปิดขั้นนี้แทนได้ ต้องกดผ่าน Console เอง

### 1. ติดตั้ง Firebase CLI

ติดตั้ง Firebase Command Line Interface (CLI) ที่ใช้จัดการ Firestore และ Hosting จากบรรทัดคำสั่ง

```
npm install -g firebase-tools
```

คำสั่งนี้ติดตั้งเพียงครั้งเดียวบนเครื่อง ใช้ได้กับทุกโปรเจกต์ Firebase บนเครื่องนี้

### 2. ล็อกอิน Firebase

เปิดเบราว์เซอร์เข้าบัญชี Google เดียวกันกับที่ใช้สร้าง Firebase project ในสัปดาห์ที่ 6 แล้วรันคำสั่ง

```
firebase login
```

ระบบจะเปิดหน้าเบราว์เซอร์ขอการอนุญาต — กดยืนยันให้สิทธิ์ Firebase CLI

### 3. เชื่อมต่อโปรเจกต์

เปิดหน้าต่าง Terminal/Command Prompt ในโฟลเดอร์ `leaveeasy-oneshot` แล้วรันคำสั่ง

```
firebase use --add
```

ระบบจะแสดงรายชื่อโปรเจกต์ Firebase ทั้งหมด — เลือกโปรเจกต์ที่สร้างไว้ในสัปดาห์ที่ 6 ตั้งชื่อ alias เป็น `default` (หรือตามที่ต้องการ)

**หรือวิธีเร็วกว่า:** แก้ไขไฟล์ `.firebaserc` ในโฟลเดอร์นี้แล้วแทนที่ `YOUR_FIREBASE_PROJECT_ID` ด้วยรหัสโปรเจกต์จริง (ดูได้จากหน้า Firebase Console มุมบนสุด)

### 4. ปรับใช้กฎความปลอดภัย

ไฟล์ `firestore.rules` มีกฎขั้นต่ำที่บังคับให้ผู้ใช้ล็อกอินก่อนจึงจะอ่านและเขียนข้อมูลได้ ปรับใช้กฎเหล่านี้ลงฐานข้อมูล Firestore

```
firebase deploy --only firestore:rules
```

### 5. ปรับใช้เว็บไซต์

นำหน้าเว็บและไฟล์ต่างๆ ขึ้นเซิร์ฟเวอร์ Firebase Hosting เพื่อให้ผู้อื่นเข้าถึงได้ผ่าน URL ที่สาธารณะ

```
firebase deploy --only hosting
```

ระบบจะแสดง URL ที่สาธารณะเมื่อเสร็จ — ดูเหมือนว่า `https://your-project-id.web.app`

### 6. ตรวจสอบว่าทำงานได้

- เปิด URL ที่แสดงจากขั้นที่ 5 — ควรเห็นหน้าล็อกอินแทนหน้าแรกของระบบ (เพราะ Security Rules บังคับให้ล็อกอินก่อน)
- สมัครสมาชิกบัญชีใหม่ในระบบ
- เข้าล็อกอินด้วยบัญชีที่สมัครไป — ควรเห็นหน้าแรก (index.html) หรือรายการใบลา ขึ้นอยู่กับการออกแบบว่าหลังล็อกอินจะไปหน้าไหน
- ยืนยันว่าเห็นรายการใบลา 5 ใบที่เป็นข้อมูลตัวอย่างจากสัปดาห์ที่ 6

### 7. ขอเพื่อนร่วมเรียนทดสอบ (หรือเปิดหน้าต่างนิรนามที่ถูก)

- ขอเพื่อนคนอื่นหรือผู้สอนเปิด URL ที่สาธารณะเดียวกัน (โดยไม่ให้รหัสผ่านของคุณ)
- ผู้เข้าชมหากินควรเห็นหน้าล็อกอินเหมือนกัน (ไม่เห็นข้อมูลของคุณ)
- **หรือ** เปิดหน้าต่างเบราว์เซอร์ "ใหม่" แบบไม่เก็บประวัติ (Incognito/Private) ที่ URL เดียวกัน — ควรเห็นหน้าล็อกอินเหมือนกัน ซึ่งพิสูจน์ว่าเว็บไซต์เปิดให้คนอื่นเข้าถึงได้จริง ไม่ใช่แค่คุณเหมือนก่อนหน้า

---

## ถ้าเจอปัญหาในสัปดาห์ที่ 7

- `firebase login` ไม่ได้เชื่อมต่อ → ตรวจว่า npm ติดตั้งแล้ว (รันคำสั่ง `node --version` ดูว่าได้ผล) และมีการเชื่อมต่ออินเทอร์เน็ต
- `firebase use --add` ไม่เห็นโปรเจกต์ → ล็อกอิน Firebase ด้วยบัญชี Google เดียวกันกับที่ใช้สร้างโปรเจกต์
- URL ที่ได้หลังจาก `firebase deploy --only hosting` ไม่เปิดได้ → รอสักครู่แล้วลองอีกครั้ง (อาจใช้เวลาสักนิดถึงสองนาทีในการปรับใช้)
- ล็อกอินแล้วยังเห็นหน้าล็อกอินวนไป → ตรวจไฟล์ `js/auth.js` ว่ามีการเปลี่ยนหน้าหลังจากล็อกอินสำเร็จหรือไม่
- ข้อมูลตัวอย่างไม่ปรากฏเมื่อล็อกอิน → ตรวจว่าข้อมูลตัวอย่างถูกใส่ (จาก `seed.html` ในสัปดาห์ที่ 6) ก่อนปรับใช้ Security Rules
